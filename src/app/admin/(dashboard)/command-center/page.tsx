import ChatWorkspace from "@/components/admin/command-center/ChatWorkspace";

export const metadata = { title: "Command Center — Reelo Admin" };

export default function CommandCenterPage() {
  return <ChatWorkspace />;
}
